# 연산자 벽돌깨기 - 구현 계획 (2026-08-04)

가칭: **옵브릭 (OpBricks)** / 폴더: `~/snowball/opbricks/`
전략: 웹 프로토타입(단일 HTML)으로 재미 검증 → 통과 시 앱 래핑

---

## 1. 게임 규칙 v0 (프로토타입 기준)

### 보드
- 세로 화면, 논리 해상도 420×740 고정 (실제 화면엔 비율 유지 스케일 + DPR 대응)
- 벽돌 격자 **7열**, 셀 60×60px, 위에서부터 쌓임
- 바닥 위 경고선(발사대 바로 위): 벽돌이 이 선에 닿으면 게임오버

### 벽돌 2종
| 종류 | 표기 | 동작 |
|---|---|---|
| HP 벽돌 | 숫자 (예: 12) | 공 1회 충돌마다 -1, 0이 되면 파괴 + 점수. 공은 반사됨 |
| 연산 벽돌 | `×2` `+10` `-30` | 공이 닿으면 **1회 발동 후 소멸**, 공은 통과 |

### 연산 규칙 (핵심 반전)
- `×2`: 발동 순간 **필드의 활성 공 전부 즉시 분열** (각도 살짝 틀어서)
- `+N`: 발사대에서 공 N개 추가 발사
- `-N`: 활성 공 N개 즉시 회수(제거)
- 턴 종료 시 **살아남아 회수된 공 개수 = 다음 턴 발사 수** → 공이 자원
- 순서가 실력: `-30` 먼저 맞고 `×2` = (n-30)×2, 반대면 n×2-30
- 성능 캡: 활성 공 최대 **512개**. 캡 상태의 ×2는 보너스 점수로 전환

### 턴 흐름
```
조준(드래그) → 연사(0.05s 간격) → 물리 시뮬 → 바닥 착지 시 회수
→ 전 줄 1칸 하강 + 새 줄 상단 생성 → 게임오버 체크 → 다음 턴
```
- 첫 번째로 바닥에 닿은 공의 x좌표 = 다음 턴 발사 위치 (장르 표준)
- 새 줄 생성 규칙: 라운드가 오를수록 HP 벽돌 숫자↑, 연산 벽돌은 확률 배치 (시드 기반)

### 점수
- 점수 = 파괴한 벽돌 HP 총합 + 캡 초과 보너스
- 기록 = 라운드 수 + 최대 보유 공 수 ("공 340개까지 불림")

## 2. 출퇴근 요구사항 → 구현 매핑
| 요구 | 구현 |
|---|---|
| 한 손 조작 | 화면 하단 아무 데나 드래그 → 조준선 표시, 놓으면 발사 |
| 흔들림 보정 | 드래그 거리가 길수록 각도 변화 미세 (거리 비례 감도 감소) |
| 무음 | 오디오 없음. 피드백 = `navigator.vibrate` + 파티클/플래시 |
| 언제든 중단 | 턴제 + 자동 저장 |
| 오프라인 | 서버·로그인 없음, localStorage만 |
| 판 복원 | 벽돌 배열·보유 공·라운드·RNG 상태 직렬화, `visibilitychange`마다 저장 |
| 지루함 방지 | 공 비행 3초 후 2배속, 8초 후 4배속 + "일괄 회수" 버튼 |

## 3. 기술 결정
- **순수 JS + Canvas 2D, 단일 `index.html`** (라이브러리·빌드 없음 → 폰에서 파일 하나로 테스트)
- 물리: 직접 구현. 원-AABB 충돌 + 반사. **고정 타임스텝(120Hz) + 이동거리 기반 서브스텝**으로 고속 공 터널링 방지
- RNG: mulberry32 (시드 기반) → 데일리 모드·저장 복원에 필수. `Math.random()` 사용 금지
- 저장: localStorage 키 `opbricks.save.v1` (버전 붙여 마이그레이션 대비)
- 데일리 시드 = `YYYYMMDD` (KST 기준)

## 4. 구현 단계 (각 단계마다 브라우저로 동작 확인 후 다음 진행)

### 1단계. 뼈대 (반나절)
- 캔버스 셋업, DPR 스케일, 세로 레터박스, 메인 루프(고정 타임스텝)
- 격자·발사대·경고선 렌더, 더미 벽돌 표시
- ✅ 검증: 폰 비율에서 화면이 안 깨지고 60fps

### 2단계. 물리 코어 (1일) - 최대 리스크 구간
- 공 이동, 좌우·상단 벽 반사, 바닥 도달 판정
- 원-AABB 충돌: 최근접점 방식 + 어느 면에 맞았는지로 반사축 결정
- 서브스텝 터널링 방지, 모서리 충돌 처리
- 수평 고착 방지: 반사각이 수평에 가까우면 최소 각도 보정 (장르 필수 처리)
- ✅ 검증: 공 1개를 극단 각도로 쏴서 뚫림·끼임·무한루프 없음

### 3단계. 턴 시스템 (1일)
- 드래그 조준(조준선 점선 + 첫 반사 예측), 감도 감소 보정, 최소 발사각 제한(수평 발사 금지)
- 연사 → 회수 → 첫 착지점이 다음 발사 위치
- 줄 하강·새 줄 생성(시드 RNG), HP 감소·파괴, 게임오버·재시작
- ✅ 검증: HP 벽돌만으로 원조 장르 수준의 한 판이 완주됨

### 4단계. 연산자 벽돌 = 차별화 (1일)
- `×2` 분열(캡 512 + 초과분 점수 전환), `+N` 추가 발사, `-N` 회수
- 배치 밸런스 1차: 초반 ×2 후하게(쾌감 학습) → 중반부터 -N 섞어 순서 고민 유발
- 연산 발동 순간 연출(벽돌 팝 + 숫자 플로팅 + 진동)
- ✅ 검증: "-30 피해서 ×2 먼저" 같은 의도적 플레이가 실제로 점수 차이를 만드는가
- ⚠️ 여기서 재미없으면 수치·배치를 갈아엎는 구간. 일정 여유 둘 것

### 5단계. 게임필 + UX (반나절)
- 파티클, 파괴 플래시, 공 개수 카운터 애니메이션, 배속·일괄회수 버튼
- 진동 패턴(파괴 약하게 / ×2 강하게), 무음 확인
- 게임오버 화면: 점수·라운드·최대 공, 재도전
- ✅ 검증: 소리 없이도 ×2 연쇄가 "터졌다"고 느껴지는가

### 6단계. 저장·복원 (반나절)
- 판 상태 직렬화(벽돌·보유 공·라운드·점수·RNG 상태), 자동 저장, 재접속 복원
- 공 비행 중 이탈 시: 해당 턴 발사 직전 상태로 복원 (비행 중 스냅샷은 안 함 - 단순화)
- ✅ 검증: 탭 강제 종료 → 재접속 → 같은 판·같은 시드 줄 생성 재현

### 7단계. 데일리 + 공유 (반나절)
- 데일리 모드: 날짜 시드, 하루 1판, 결과 고정
- 공유 문자열: `옵브릭 8/5 🧱 R14 · 공 340개 ×2×2×2` + 링크, 클립보드 복사
- 무한 모드 분리(랜덤 시드)
- ✅ 검증: 같은 날짜 = 두 기기에서 같은 판

### 8단계. 폰 실테스트 + 밸런스 (반나절~)
- 사용자 폰으로 테스트 (WSL이라 로컬 `python3 -m http.server` 또는 파일 전달)
- 흔들림 보정 감도, 공 512개 시 프레임 확인, 한 판 2~3분인지 측정
- 밸런스 상수는 코드 상단 `CONFIG` 객체에 모아 튜닝 반복

## 5. 프로토타입 범위 제외 (검증 통과 후)
- 광고·부활(확정 1회), 앱 래핑(자체등급분류), 랭킹, 아이템/스킨, 사운드 옵션
- 배포·git은 사용자 직접 수행 (빌드/파일 준비까지만)

## 6. 리스크
| 리스크 | 대응 |
|---|---|
| 고속 공 터널링/끼임 | 서브스텝 + 최소각 보정, 2단계에서 극단 테스트 |
| 공 512개 성능 | 단순 원 렌더(이미지 X), 충돌 그리드 해싱은 필요 시만 |
| ×2 인플레로 밸런스 붕괴 | 캡 + -N 벽돌 + 새 줄 HP를 라운드 비례 상승 |
| 턴이 늘어져 지루 | 배속 + 일괄 회수 버튼 (5단계) |
